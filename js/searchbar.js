const searchButton = document.getElementById("buton_search");

searchButton.addEventListener("click", function (event) {
  event.preventDefault();

  alert("Functia de search este in mentenanta!");
});
